# GitHub Agent Environment Variables Template
# Bu dosyayı .env olarak kopyalayın ve kendi değerlerinizi girin
# cp .env.template .env

# ==========================================
# OpenAI Configuration (ZORUNLU)
# ==========================================
# OpenAI API anahtarınızı buraya girin
OPENAI_API_KEY=sk-your-openai-api-key-here

# Kullanılacak OpenAI model (opsiyonel, varsayılan: gpt-4)
OPENAI_MODEL_ID=gpt-4

# Maksimum token sayısı (opsiyonel, varsayılan: 4000)
OPENAI_MAX_TOKENS=4000

# ==========================================
# GitHub Configuration 
# ==========================================
# GitHub token (GitHub Actions'da otomatik ayarlanır, yerel test için gerekli)
# GITHUB_TOKEN=ghp_your_github_personal_access_token

# Repository bilgisi (GitHub Actions'da otomatik ayarlanır)
# Format: owner/repository-name
# GITHUB_REPOSITORY=kullanici-adi/repo-adi

# GitHub event tipi (GitHub Actions'da otomatik ayarlanır)
# GITHUB_EVENT_NAME=workflow_dispatch

# GitHub kullanıcı adı (GitHub Actions'da otomatik ayarlanır)
# GITHUB_ACTOR=kullanici-adi

# ==========================================
# Strands Agents Configuration
# ==========================================
# Tool onaylarını bypass et (önerilen: true)
BYPASS_TOOL_CONSENT=true

# Console mod (önerilen: enabled)
STRANDS_TOOL_CONSOLE_MODE=enabled

# ==========================================
# Development & Debugging
# ==========================================
# Debug modu (development için)
DEBUG=false

# Log seviyesi (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Agent davranış modu (normal, verbose, quiet)
AGENT_MODE=normal

# ==========================================
# Agent Customization (Opsiyonel)
# ==========================================
# Agent'a özel mesaj (varsayılan mesajı override eder)
# AGENT_MESSAGE="Özel agent mesajınız buraya"

# Agent dili (tr, en)
AGENT_LANGUAGE=tr

# Agent yanıt formatı (markdown, plain, json)
AGENT_RESPONSE_FORMAT=markdown

# ==========================================
# Local Development Settings
# ==========================================
# Yerel test için kullanılacak GitHub repository
# DEV_GITHUB_REPOSITORY=test-user/test-repo

# Yerel test için GitHub event simulasyonu
# DEV_GITHUB_EVENT_NAME=issues
# DEV_GITHUB_ACTION=opened

# ==========================================
# Security Settings
# ==========================================
# Güvenlik seviyesi (strict, normal, permissive)
SECURITY_LEVEL=normal

# İzin verilen GitHub kullanıcıları (virgülle ayrılmış)
# ALLOWED_USERS=user1,user2,user3

# İzin verilen GitHub organizasyonları
# ALLOWED_ORGS=org1,org2

# ==========================================
# Performance Settings
# ==========================================
# HTTP request timeout (saniye)
HTTP_TIMEOUT=30

# Maksimum retry sayısı
MAX_RETRIES=3

# Rate limiting (requests per minute)
RATE_LIMIT=60

# ==========================================
# Notification Settings (Gelecek özellikler)
# ==========================================
# Slack webhook URL (ileride kullanılabilir)
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...

# Discord webhook URL (ileride kullanılabilir)
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# Email notifications (ileride kullanılabilir)
# NOTIFICATION_EMAIL=your-email@example.com

# ==========================================
# Example Values (Referans için)
# ==========================================
# OPENAI_API_KEY=sk-1234567890abcdef...
# GITHUB_TOKEN=ghp_1234567890abcdef...
# GITHUB_REPOSITORY=myusername/myrepo
# DEBUG=true
# LOG_LEVEL=DEBUG
# AGENT_LANGUAGE=tr

# ==========================================
# Notlar
# ==========================================
# 1. OpenAI API key'inizi https://platform.openai.com/api-keys adresinden alabilirsiniz
# 2. GitHub Personal Access Token'ınızı https://github.com/settings/tokens adresinden oluşturabilirsiniz
# 3. Gerekli GitHub token permissions: repo, issues, pull_requests, actions
# 4. Bu dosyayı .gitignore'a eklemeyi unutmayın (zaten eklenmiş)
# 5. Production ortamında GitHub Secrets kullanın, .env dosyası sadece development içindir